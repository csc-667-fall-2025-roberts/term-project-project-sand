<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Error <%= status %> - Webopoly</title>
  <link rel="stylesheet" href="/css/styles.css">
</head>
<body class="min-h-screen flex items-center justify-center bg-gradient-to-br from-amber-50 via-orange-50 to-pink-50">
  <div class="w-full max-w-md">
    <div class="card shadow-2xl border-2 border-amber-200 text-center">
      <div class="mb-6">
        <div class="text-6xl font-black bg-gradient-to-r from-amber-600 via-orange-600 to-pink-600 bg-clip-text text-transparent mb-4"><%= status %></div>
        <h1 class="text-3xl font-bold text-gray-800 mb-2">
          <% if (status === 404) { %>
            Page Not Found
          <% } else if (status === 500) { %>
            Server Error
          <% } else { %>
            Something Went Wrong
          <% } %>
        </h1>
        <p class="text-gray-700 font-semibold mb-8"><%= message %></p>
      </div>

      <div class="space-y-3 flex flex-col sm:flex-row gap-3 justify-center">
        <a href="/lobby" class="btn-primary inline-block text-center">
          Go to Lobby
        </a>
        <a href="/auth/login" class="btn-secondary inline-block text-center">
          Back to Login
        </a>
      </div>

      <% if (error && Object.keys(error).length > 0) { %>
        <div class="mt-8 p-4 bg-gray-100 rounded text-left">
          <p class="text-xs font-mono text-gray-600 break-all">
            <%= JSON.stringify(error, null, 2) %>
          </p>
        </div>
      <% } %>
    </div>
  </div>
</body>
</html>

